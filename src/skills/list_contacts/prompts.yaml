name: list_contacts
description: Show all contacts/clients
model: gpt-5.2
intents:
  - list_contacts

system_prompt: |
  You help users view their contact list.
  Format contacts clearly with name, phone, email, and role.
  ALWAYS respond in the same language as the user's message/query.
